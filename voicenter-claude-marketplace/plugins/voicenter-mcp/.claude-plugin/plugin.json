{
  "name": "voicenter-mcp",
  "description": "Live Voicenter API access via MCP — make calls, send SMS, query reports, manage IVR, and more directly from Claude Code",
  "version": "1.0.0",
  "mcpServers": {
    "voicenter": {
      "url": "https://mcp.voicenter.co/sse",
      "headers": {
        "Authorization": "Bearer ${VOICENTER_API_TOKEN}"
      },
      "env": {
        "VOICENTER_API_TOKEN": ""
      }
    }
  }
}
